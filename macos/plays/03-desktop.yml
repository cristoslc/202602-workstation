---
- name: "Phase 3: Desktop environment"
  hosts: localhost
  connection: local

  roles:
    - role: raycast
      tags: [raycast, desktop]

    - role: mas
      tags: [mas, desktop]

    - role: macos-defaults
      tags: [macos-defaults, desktop]
      when: apply_macos_defaults | default(true)
