---
- name: "Phase 6: Bureau Veritas"
  hosts: localhost
  connection: local

  module_defaults:
    ansible.builtin.apt:
      lock_timeout: 300

  roles:
    - role: ms-office
      tags: [ms-office, office, edge, onedrive, teams, bureau-veritas]
