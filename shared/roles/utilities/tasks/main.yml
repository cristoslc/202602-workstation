---
# Utilities role: Keka (macOS) / placeholder (Linux)

- name: Verify Keka (macOS)
  when: ansible_os_family == "Darwin"
  ansible.builtin.include_tasks: darwin.yml

- name: Note about utilities (Debian)
  when: ansible_os_family == "Debian"
  ansible.builtin.debug:
    msg: "Archive utilities on Linux are handled by base packages."
