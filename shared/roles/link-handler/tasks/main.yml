---
# Link handler role: file and URL routing to the correct application.
# macOS: OpenIn (Setapp-managed).
# Linux: xdg-open + custom MIME associations, or Junction as GUI alternative.

- name: Include OS-specific tasks
  ansible.builtin.include_tasks: "{{ ansible_os_family | lower }}.yml"
