---
docker_mcp_profile_name: workstation

docker_mcp_servers:
  - brave-search
  - context7
  - dockerhub
  - duckduckgo
  - markdownify
  - playwright

# Brave Search API key — set in shared/secrets/vars.sops.yml as:
#   docker_mcp_brave_api_key: "your-key-here"
docker_mcp_brave_api_key: ""
