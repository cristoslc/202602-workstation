---
# Enable sshd on SteamOS.
# On SteamOS, sshd is disabled by default and may be re-disabled after OS updates.
# This task is idempotent — safe to re-run at any time.

- name: Enable and start sshd
  ansible.builtin.systemd:
    name: sshd
    enabled: true
    state: started
  become: true
