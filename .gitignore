# Decrypted secrets (created at bootstrap time, never committed)
.decrypted/
*.decrypted

# Age private keys
keys.txt
*.age-key

# Logs
first-run.log
bootstrap.log
*.log

# Ansible
*.retry
.become-*

# Local override files (composable dotfiles — user-created, never stowed)
local.zsh
local.gitconfig
local.yml

# Plaintext personal files (age-encrypted copies live in macos/files/)
macos/dotfiles/iterm2/.config/iterm2/com.googlecode.iterm2.plist
macos/files/stream-deck/plugins.json
macos/files/stream-deck/streamdeck-plugins.html
macos/files/openin/openin.plist

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Dolt database files (added by bd init)
.dolt/
*.db
